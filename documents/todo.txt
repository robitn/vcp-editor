✅ Add actual drag-and-drop functionality for repositioning elements
✅ Implement Undo/Redo stack (5 levels as per spec)
✅ Add toolbar buttons for creating new borders/images/buttons
✅ Implement Cut/Copy/Paste operations
✅ Add file browser dialog for image paths and button folders
✅ Save Window size and position
✅ Add keyboard shortcuts (Cmd+S for save, etc.)
✅ Test with the sample VCP file provided
✅ Add visual feedback for multi-cell drag selection
✅ Implement element deletion
✅ Add Print... to File menu to print VCP image via OS native print dialog
✅ Add validation and error handling UI
✅ Change window title to document <filename>
✅ Change Application menu name to "VCP Editor"
✅ Warn of unsaved changes, allow saving
✅ Remember last file opened
✅ Settings... UI/UX
✅ Can Open dialog be clued where to open to based on context? Yes - uses last file dir, VCP resources folder, or home dir
✅ the save-folder location should honor the folder structure: (save-folder) -> skins, (save-folder) -> images, (save-folder) -> Buttons, (save-folder) -> Buttons -> [button-folders].
✅ Settings labels trigger a finger cursor but clicking does nothing
✅ Settings File "Default save location" should have a folder browser button
✅ Settings settings are typically auto-saved. Auto-save changes and remove "Save" button. Change "Cancel" button text to "Close"
✅ Add (empty, but developer editable) "attributions" box to About box.
✅Contextual menus:
    - Selected empty cell > Border, Button, Image
    - Select occupied cell > Border, -, Cut, Copy, Paste, Delete

✅ Font library for icons - restyle toolbar buttons 
✅ Versioning for the About
✅ It's become difficult to select the border for editing. -fixed

Inspector -> Border -> Fill Color should be a ColorPicker control + Transparent.
Add color tags to VCP Output pane.
Sort buttons by row then column.
Generate a user manual.


### Version 2 Features:
Multi tab window.

Add Open Recent > menuitem

Because images and buttons may be created by an outside developer. A floating or fixed palette of images and buttons from a (root) folder. Drag-n-Drop.




----
Terminal:
lsof -ti:1420 | xargs kill -9 2>/dev/null || echo "No process found on port 1420"
